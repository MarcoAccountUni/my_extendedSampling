# Softmax implementation

In this directory, you will find the code (in `code/`) to run Monte Carlo simulations in the extended space of amino acid sequences, and snippets of code (in `monte_carlo/`) to use them. 
